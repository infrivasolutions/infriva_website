import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact | Infriva Solutions",
  description:
    "Contact Infriva Solutions for website development, CRM development, SEO, Meta Ads, WhatsApp API, automation and lead generation services.",
};

export default function ContactPage() {
  return <ContactClient />;
}
